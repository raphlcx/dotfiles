#!/usr/bin/env bash

# System upgrade script

# Arch system upgrade
sudo pacman -Syyu
sudo pacman -Sc --noconfirm

# tldr local database
tldr -u

# vim plugins
git -C ~/.vim submodule foreach git pull origin master
