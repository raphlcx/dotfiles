#!/usr/bin/env bash

# Update brew and cask
brew update \
  && brew upgrade \
  && csc \
  && brew cleanup -s \
  && brew prune \
  && brew cask cleanup

# Update tldr
echo 'Updating tldr local database ...'
tldr -u
